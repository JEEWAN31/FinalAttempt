#include<iostream>
#include<bit/stdc++.h>

using namespace std;

struct ListNode {
    int val;
    ListNode *next;
    ListNode() : val(0), next(nullptr) {};
    ListNode(int x) : val(x), next(nullptr) {};
    ListNode(int x, ListNode *next) : val(x), next(next) {};
}

int main(){
    
}

// I am having to Delete element from a LinkedList. There are 3 ways to delete an elememt into it
// 1. Delete at the beginning


// 2. Delete at the end


// 3. Delete at a given position or a given element